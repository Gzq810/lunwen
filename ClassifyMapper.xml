<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ClassifyMapper">

    <select id="selectAll" resultType="com.example.entity.Classify">
        select * from `classify`
        <where>
            <if test="name != null"> and name like concat('%', #{name}, '%')</if>
        </where>
        order by id desc
    </select>

    <delete id="deleteById">
        delete from `classify`
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Classify" useGeneratedKeys="true">
        insert into `classify`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="img != null">img,</if>
            <if test="introduce != null">introduce,</if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="img != null">#{img},</if>
            <if test="introduce != null">#{introduce},</if>
        </trim>

    </insert>

    <update id="updateById" parameterType="com.example.entity.Classify">
        update `classify`
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="img != null">
                img = #{img},
            </if>
            <if test="description != null">
                introduce = #{introduce},
            </if>
        </set>
        where id = #{id}

    </update>

</mapper>